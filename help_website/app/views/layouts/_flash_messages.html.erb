

<% flash.each do |notice, message| %>
  <div class="alert <%= bootstrap_class_for(notice) %> fade in">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>